# Simple-Calculator
a simple calculator with error handling. It accept the user input (two numbers). Let them choose an operator,(given options to choose from. Error handling: incase the user divides by zero or if they insert a text instead of numbers 
